<h1><%= @category.name %></h1>
<p><%= @category.description %></p>

<%= link_to 'Edit Category', edit_category_path(@category), class: "button" %> |
<%= link_to "Delete Category", @category, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>